<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Camera feature (not required, app works without it) -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Wallet_wise_app">

        <!-- Login Activity (Launcher / First Page) -->
        <activity
            android:name=".screens.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Register Activity -->
        <activity
            android:name=".screens.RegisterActivity"
            android:exported="false" />

        <!-- Set Goals Activity -->
        <activity
            android:name=".screens.SetGoalsActivity"
            android:exported="false" />

        <!-- Create Expense Activity -->
        <activity
            android:name=".screens.CreateExpenseActivity"
            android:exported="false" />

        <!-- Expense List Activity -->
        <activity
            android:name=".screens.ExpenseListActivity"
            android:exported="false" />

        <!-- View Goals Activity -->
        <activity
            android:name=".screens.ViewGoalsActivity"
            android:exported="false" />

        <!-- Create Category Activity -->
        <activity
            android:name=".screens.CreateCategoryActivity"
            android:exported="false" />

        <!-- View Categories Activity -->
        <activity
            android:name=".screens.ViewCategoriesActivity"
            android:exported="false" />

        <!-- Projection Calendar Activity -->
        <activity
            android:name=".screens.ProjectionCalendarActivity"
            android:exported="false" />

        <!-- FileProvider for Camera Photos -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>